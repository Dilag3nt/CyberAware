{
  "name": "dilag3nt",
  "version": "1.0.0",
  "scripts": {
    "prebuild": "cp static/style.css dist/ || true",
    "build": "parcel build static/core.js --public-url ./dist/"
  },
  "devDependencies": {
    "parcel": "^2.7.0"
  }
}